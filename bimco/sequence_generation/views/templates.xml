<odoo>
    <template id="report_purchasequotation_document_inherited"
              inherit_id="purchase.report_purchasequotation_document">
        <xpath expr="t/div/h2[hasclass('mt-4')]" position="replace">
            <h2 class="mt-4">Purchase Request
                <span t-field="o.name"/>
            </h2>
        </xpath>
    </template>

</odoo>